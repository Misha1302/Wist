import "Array.wst"
import "Types.wst"
import "Enumerator.wst"
import "WistTime.dll"

import "WistTime.dll"

func Start() 

{
    let n = new Number( // here will be an syntax error (parenthesis not closed)
    // and if you can run this code you catch error: can't found Ctor0
    
    Print(n)
}

class Number(n)
 
{
 
}
