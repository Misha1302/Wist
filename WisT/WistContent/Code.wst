import "Array.wst"
import "Types.wst"
import "Enumerator.wst"
import "WistTime.dll"

func Start() {
    let en = new Enumerator(new Array([1, 2, new Array([5, 1120]), "?", "Hello", new Array([5, 120]), "HI!"]))
    
    while en.MoveNext() {
        Print(en.Current())
    }
}