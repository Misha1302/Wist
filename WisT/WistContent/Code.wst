import "Array.wst"
import "Types.wst"
import "WistTime.dll"

func Start() {
    loop let q = 0; q < 10; q += 1 {
        let t = GetTime()
        
        loop let i = 0; i < 10_000_000; i += 1 { }
        
        Write(GetTime() - t)
        Write("+")
    }
    Print("\b ")
}