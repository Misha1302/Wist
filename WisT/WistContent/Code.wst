import "Array.wst"
import "Types.wst"
import "Enumerator.wst"
import "WistDebugHelper.dll"
import "WistFiles.dll"

func Start() {
    let firstFuncAddr = F&1
    let secondFuncAddr = F&2
    Print(firstFuncAddr)
    Print(secondFuncAddr)
    
    Invoke(secondFuncAddr, ['Hi ', 54])
}

func F(n) {
    Print(n)
}

func F(n, n2) {
    Print(ToStr(n) + ToStr(n2))
}