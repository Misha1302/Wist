import "WistRandomLib.dll"
import "WistTime.dll"
import "Array.wst"

func Start() {
    MyPrint(1)
    MyPrint(1, 2)
    MyPrint(1, 2, 3)
    
    let arr = new Array()
    let arr2 = new Array([1,2,3])
    
    Print(arr)
    Print(arr2)
}

func MyPrint(a) {
    Print(a)
}

func MyPrint(a, b) {
    Print(ToStr(a) + ToStr(b))
}

func MyPrint(a, b, c) {
    Print(ToStr(a) + ToStr(b) + ToStr(c))
}